var tab = [-2,1,4];
var first = tab[0];
var last = tab[tab.length -1];  

function additionne (x){
    return x + 2;
}

function affiche (){
    alert(additionne(first));
    alert(additionne(last));
}